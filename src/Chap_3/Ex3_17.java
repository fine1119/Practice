package Chap_3;

public class Ex3_17 {
    public static void main(String[] args) {
        // 조건 연산자 : (조건식) ? 식1 : 식2
        // 조건식이 true면 식1이 연산결과가 되고, false면 식2가 연산결과가 된다.
        // if문으로 쓰는 것보다 간결하게 표현할 수 있는 장점

        // 표현방법1 (조건 연산자)
        // result = (x > y) ? x : y;

        // 표현방법2 (if 조건문)
        /* if (x > y)
                result = x;
            else
                result = y;
         */
    }
}
